# Experiment result statuses

Use `tests/pentax_stability_result_statuses.txt`.

`INCONCLUSIVE` is preferable to inventing a pass/fail when instrumentation is insufficient. `BLOCKED_UNSAFE` is valid when continuing would risk camera/data integrity; document what was learned before stopping.

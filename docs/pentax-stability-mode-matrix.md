# Mode-aware hardware matrix

See `tests/pentax_stability_mode_matrix.csv`.

The table schedules measurements; it does not encode completion formulas. For every row, actual camera conditions, busy duration and candidate descriptors are the evidence.

`record` means preserve the camera's actual setting rather than silently forcing an assumption. Pixel Shift + NR is executed only if the body/configuration permits the combination safely.

# Execution handoff

The design phase has deliberately stopped before speculative runtime changes.

Camera-connected agent: take ownership from `pentax-stability-next-agent-prompt.md`. First produce completed static audits and source trace insertion points. Then execute the minimum physical-camera run. Post evidence to focused issues + #82 coordination.

Human/solution-architecture review should intervene if an agent proposes a broad timeout/recovery rewrite without a reproduced first-divergence trace.

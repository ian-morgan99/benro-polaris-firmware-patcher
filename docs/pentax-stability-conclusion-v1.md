# v1 design conclusion

The current plan is broad enough to test whether crashes originate in capture finalisation, legitimate long-mode handling, command races, process/session state, USB, preview/network or embedded integration — rather than assuming recovery is the primary problem.

The correct next move is now empirical: source audits + instrumentation + physical K-3 III experiments.

# Diagnostic layer ladder

See `tests/pentax_stability_test_layers.csv`.

Move up the ladder only after the lower layer is characterised for the scenario. Move down a layer when a higher-layer failure needs attribution.

The primary first-cause path is Polaris pgphoto + staged libgphoto2, not OpenPolaris and not a desktop-only control.

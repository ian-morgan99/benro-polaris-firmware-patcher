# Stability test-plan contract

The machine-readable test files are executable intent, not measured results. `NOT_RUN`, `TO_MEASURE`, `UNKNOWN` and `TO_VERIFY` must remain explicit until evidence replaces them.

Agents must not convert placeholders to values based solely on documentation assumptions.

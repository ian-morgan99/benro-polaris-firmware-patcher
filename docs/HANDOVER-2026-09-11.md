# Polaris camera stack handover — 2026-09-11

## State at handover

`UNCLAIMED — HANDOVER READY`. No agent owns the camera or Polaris. Do not
qualify results until the device is observed, the installed firmware is
identified, and competing preview/camera clients are stopped.

| Layer | Current revision / artifact | Verified status |
|---|---|---|
| OpenPolaris | `5432c86f` on `main` | Full Gradle `check` passed; rolling Android build passed; Windows job was still running when this handover was written; iOS has no app shell and produces no IPA (tracked as OpenPolaris #78). |
| libgphoto2 fork | `90de508a5441cb7e2b1b02d601b15a7547d7bcee` on `master` | Exact-model dual-camera audit passed preview on both bodies and fixed duplicate focus-alias dispatch. Ninja build and targeted Pentax tests passed. |
| patcher | `af5d7d5` on `main` at build time | Full builds fail closed without explicit libgphoto2 source. Source/provenance tests pass. |
| firmware | `o-v9d-model-aware-focus` in private `ian-morgan99/PrivateResearch` | Build-validated only; never installed or physically qualified on Polaris. |
| test harness | `f6870c2` on `main` | 61/61 tests pass; reusable skill is `.github/skills/polaris-test-harness/SKILL.md`. |

## Candidate identity

- Private path: `firmware-packets/o-v9d-model-aware-focus/FwPkt.zip`
- Zip MD5: `90968076a1841e46017372a390277a0d`
- Zip SHA-256: `f673123958e0d09576633a113850e7e1226759910528675957ca3b53be016e8e`
- Appfs MD5: `1068c3b99272f99b9c0d3f033960aee5`
- Source input: clean libgphoto2 `90de508a5441cb7e2b1b02d601b15a7547d7bcee`
- Building patcher: `af5d7d5`
- Provenance records `vanilla_source_explicit=0` and an empty dirty hash.

The structural ZIP, `firmwareInfo`, ARM soft-float ABI, glibc ceiling,
core/port boundary, required Pentax model strings, Stage-2 content, and
corresponding-source gates passed. This is not physical camera evidence.

## What changed after the installed v7 build

1. The Stage-2 Pentax model gate was corrected, so the preview keep-live-view
   and backoff shims can engage for the intended Pentax identities (patcher
   #56). This remains unqualified until o-v9d is installed and its Stage-2 log
   lines are observed.
2. Generic `manualfocusdrive` registration remains, backed by the existing
   model-aware Pentax paths. The unverified `autofocusdrive` and scalar
   `imageformat` guesses were removed before o-v9c was built.
3. OpenPolaris has guarded Benro focus-jog serialization for commands 262 and
   311. Explicit `ret >= 0` is required; a protocol reply alone is not success.
4. Full firmware builds cannot silently fall back to vanilla libgphoto2.

## Latest retained device evidence

The latest downloaded pair is
`docs/evidence/k1ii-k3iii-2026-09-10-v7/polaris-logs/Mlog_000008.log`
and `Clog_000007.log`. Despite the directory name, that interval contains K-01
followed by K-1 II; the K-3 III was connected afterwards.

- K-01 initialized, reported connected, and then disconnected.
- K-1 II reached `sp_Gphoto_Init ret 0`, then accumulated 589
  `waitCameraIdle busy` and 2,049 `get_single_config ... failed: -2` entries.
- Mlog recorded 215 `SP_ClientCtxDel:not find` events.
- Protocol replies continued until the end, so this evidence does not show a
  total polestar process crash.
- Earlier route/service evidence showed SSH port 22 and control port 9090 open.
  Recheck both live before relying on that historical observation. MJPEG
  preview is a separate port/path and must be proven with non-empty JPEGs.

These findings are logged on patcher #50 and libgphoto2 #50. The K-3 III
radio-starvation symptom is tracked on patcher #55; v7 cannot validate its
fix because the model-gate defect kept the relevant shim inactive.

## Exact resume order

1. Claim the hardware in the live-progress ledger and record time, camera body,
   USB personality, installed firmware identity, Wi-Fi interface/route, and
   listeners on ports 22 and 9090.
2. Preserve current Mlog/Clog and installed-file hashes before changing the
   device. Confirm the stock recovery package remains available.
3. Fetch o-v9d from PrivateResearch and independently verify all three hashes
   above. Do not use o-v9, o-v9b, o-v9c, or an unregistered local ZIP.
4. Install only by the sanctioned SD-card/cold-boot firmware flow. Do not copy
   replacement runtime libraries into `/app` over SSH.
5. After reboot, prove installed Stage-2 hashes and look for the corrected
   Pentax model gate plus preview-backoff/keep-live-view activation lines.
6. Test one body at a time: K-01, K-1 II, then K-3 III. For each body record
   connect/disconnect, ten cold and ten warm sessions where required, config
   reads, preview start/stop/restart with non-empty JPEG size and cadence,
   capture/file delivery, focus actions, and recovery after failure.
7. Keep active PC-LV as the K-3 III focus precondition. Close or record every
   additional preview client before attributing a failure to camera firmware.
8. Mark every feature `PASS`, `FAIL`, `BLOCKED`, `NOT TESTED`, or `N/A`; attach
   evidence and open/update the owning repository issue for every distinct
   failure. A command ACK or HTTP 200 is never sufficient preview/capture proof.
9. If Wi-Fi or camera control degrades, stop the load, preserve logs, restore
   the known stock image if required, and do not continue qualification on a
   mutated or unidentified runtime.

## Remaining blockers

- o-v9d is not installed and has no physical Polaris K-01/K-1 II/K-3 III matrix.
- Patcher #55/#56 require installed log and sustained preview evidence.
- K-1 II idle/config churn and K-01 disconnect/preview behavior remain open.
- Pentax autofocus action and structured writing-file-format contracts still
  require exact source/trace or hardware evidence; they are deliberately not
  exposed as guessed controls.
- iOS packaging is not implemented; no IPA exists. OpenPolaris #78 owns it.

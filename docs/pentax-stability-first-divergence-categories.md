# First-divergence categories

See `tests/pentax_stability_first_divergence_categories.csv`.

Categories are for clustering evidence, not forcing certainty. Use `UNKNOWN` when ordering is insufficient. The same later symptom may occur under several categories.

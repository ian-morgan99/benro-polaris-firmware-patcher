# Next evidence requested

From camera-connected agents, the next useful evidence is:

- completed timeout/command/session static inventories;
- exact source trace insertion points;
- first E4/E5 K-3 III runs with candidate descriptors and next-shutter proof;
- if clean, E1 95/99/100/101/105 s traces.

Everything else can wait until those results discriminate the leading hypotheses.

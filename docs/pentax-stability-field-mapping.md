# Field-to-lab mapping

Use `tests/pentax_stability_field_mapping.csv` after lab fingerprints exist.

The current #82 paired Mlog/Clog evidence remains `UNMAPPED` at the initiating-cause level: it proves disconnect/reinitialisation/stale-session context but does not yet prove which lab failure recipe caused the first transition.

# Experiment naming

Use E1-E15 family IDs from the full experiment programme and stable scenario IDs from `tests/pentax_stability_scenarios.csv`.

Run IDs should add a unique repeat/build identifier without changing scenario meaning, e.g. `E4-002-20260915-r03`.

Stable naming makes field/lab trace correlation and regression history easier.

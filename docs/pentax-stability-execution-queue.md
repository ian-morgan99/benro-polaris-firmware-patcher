# Pentax stability execution queue

See `tests/pentax_stability_execution_queue.csv`.

Dependencies are intentional: long/risky camera work should not start before observability and safe-probe behaviour are understood.

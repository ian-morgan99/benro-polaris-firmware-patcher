# Pentax stability experiment status

`tests/pentax_stability_run_status.csv` is the execution register for the initial scenario catalogue.

Agents should update it from actual hardware evidence. `NOT_RUN` is deliberately distinct from PASS. Never infer a pass because a related manual test succeeded.

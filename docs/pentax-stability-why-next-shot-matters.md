# Why the next shutter matters

Many current failures are post-capture state failures. Therefore a single image saved to disk is a weak success criterion.

The next shutter tests whether the previous exposure truly left the Pentax/PTP/candidate/session lifecycle terminal. This is why important scenarios require consecutive captures and why a first-shot-only Astro failure is highly relevant to candidate/finalisation work.

# Candidate policy characterisation

See `tests/pentax_stability_candidate_policy.csv`.

Do not finalise policy until E5 shows how Pentax distinguishes stored files from tether-transfer candidates. The product requirement to preserve DNG on camera must coexist with fully terminal PTP capture state; the solution is correct reconciliation semantics, not deleting everything.

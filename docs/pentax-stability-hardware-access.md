# Physical camera access model

The physical Pentax/Polaris hardware is accessed by camera-connected development agents/workflows, orchestrated through GitHub. Chat/analysis does not imply a direct USB connection from the conversational interface itself.

Experiment instructions therefore need to be reproducible enough for an agent to execute, record and return evidence through repository issues/artifacts for subsequent analysis.

# Repository note

The Pentax stability v1 package under `docs/pentax-stability-*` and `tests/pentax_stability_*` is the current engineering basis for #82. New work should extend measured evidence rather than create a parallel recovery-only design.

# Pentax stability v1 scope

In scope:

- K-3 III first-cause capture/session stability;
- candidate finalisation;
- long-exposure/NR/Pixel Shift/Bulb liveness;
- preview/command concurrency;
- pgphoto/session/process lifecycle;
- USB separation;
- deterministic recovery;
- K-1 II follow-up validation;
- eventual OpenPolaris/Benro E2E qualification.

Out of scope for this first campaign unless evidence directly connects them:

- unrelated ttyUSB/modem failures;
- separate known physical USB-hub flapping signatures;
- general UI feature development;
- platesolve/SkyPortal/future firmware features.

Keep the investigation narrow enough that stability evidence remains interpretable.
